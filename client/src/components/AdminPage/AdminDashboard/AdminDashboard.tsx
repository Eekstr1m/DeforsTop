import s from "./AdminDashboard.module.scss";
import c from "./../AdminPage.module.scss";

export default function AdminDashboard() {
  return (
    <div className={s.wrapper}>
      <div className={c.container}>Dashboard</div>
    </div>
  );
}
